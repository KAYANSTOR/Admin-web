# ADMIN_FUNCTIONALITY_AUDIT.md

## 1. Dashboard.tsx
- **ما يظهر:** أرقام إجمالية (اشتراكات نشطة، متوقفة، عمولات معلقة، مبيعات الشهر، مبيعات اليوم).
- **ما يقرأ من Firebase:** 
  - `users` لجلب العملاء وحالتهم.
  - `collectionGroup('sales')` لجلب المبيعات.
- **ما هو Mock أو غير دقيق:**
  - `pendingCommissions` يحسب 5% بشكل ثابت بدلاً من الاعتماد على نسبة كل عميل `commission_rate`. سيتم تعديله ليكون حسابًا حقيقيًا.
- **الأزرار:** 
  - "اشتراك جديد" يحول لصفحة الاشتراكات.
  - "إضافة عميل" يحول لصفحة العملاء.
  - "العملاء" يحول إلى قائمة العملاء.
  - أزرار فلترة الفترات (اليوم/الشهر) في الرسم البياني غير مبرمجة فعلياً، فهي Mock UI.

## 2. Clients.tsx (العملاء)
- **ما يظهر:** قائمة العملاء، شريط بحث، فلتر نشط/موقوف، زر الإضافة.
- **ما يقرأ من Firebase:** `users` و `role == 'NETWORK_OWNER'`.
- **العمليات (CRUD):** 
  - CREATE يعمل بشكل سليم ويُنشئ `Auth` و `users` و `networks/info`.
  - READ يعمل ويجلب البيانات ويفلترها.
  - UPDATE غير موجود من هذه الصفحة (يتم من الملف).
  - DELETE غير موجود. 

## 3. ClientProfile.tsx (ملف العميل)
- **ما يظهر:** معلومات العميل، بيانات الشبكة، المبيعات.
- **ما يقرأ من Firebase:** `users/{UID}` و `networks/{UID}/sales`.
- **الأزرار:**
  - تفعيل/إيقاف: يعمل ويحدث Firebase.
  - تجديد اشتراك: يعمل ويحدث Firebase.
  - رسالة تحذير: يعمل ويحدث Firebase.
- **نواقص أو Mock:**
  - تعديل بيانات العميل الأساسية (الاسم، الهاتف، اسم المتجر) غير متوفر.
  - تعديل نسبة العمولة `commission_rate` غير متوفر.
  - حذف العميل غير متوفر.
  - لا توجد فلترة لمبيعات العميل في ملفه (يعرض آخر 100 فقط).

## 4. Sales.tsx (سجل المبيعات)
- **ما يظهر:** جميع مبيعات الشبكات.
- **ما يقرأ من Firebase:** `collectionGroup('sales')`.
- **نواقص أو Mock:**
  - شريط البحث لا يفلتر النتائج فعلياً (Mock).
  - أزرار الفلترة العلوية "الكل"، "الناجحة"، "المعلقة" لا تغير الحالة المعروضة.

## 5. Subscriptions.tsx (الاشتراكات)
- **ما يظهر:** قائمة العملاء وحالة اشتراكاتهم.
- **نواقص أو Mock:** 
  - مجرد واجهة قراءة فقط، النقر يحول لملف العميل. الواجهة تعمل بشكل صحيح وتجلب من Firebase.

## 6. Commissions.tsx (العمولات)
- **ما يظهر:** قائمة العملاء ونسب عمولاتهم.
- **نواقص أو Mock:**
  - الواجهة تعمل قراءة فقط.

## 7. NotificationsManager.tsx (الإشعارات)
- **ما يظهر:** إرسال إشعار.
- **العمليات:**
  - CREATE يعمل ويكتب في `app_settings` أو `users`.
  - READ غير موجود (لا يتم عرض الإشعارات السابقة).
  - DELETE للإشعارات غير موجود.

## 8. SystemSettings.tsx (إعدادات النظام)
- **العمليات:**
  - الواجهة متصلة بـ Firebase `app_settings` وتعمل العمليات (تحديث حالة التطبيق، تغيير روابط الويب فيو، تغيير الإعلان المنبثق).

## 9. Employees.tsx (الموظفين)
- **العمليات:**
  - CREATE: يحاول إنشاء الحساب ولكنه يحتوي على خطأ باستخدام `addDoc` بدلاً من `setDoc` بـ `UID` الموظف، مما يكسر الـ rules. 
  - UPDATE: متاح لتعديل الصلاحيات.
  - DELETE: متاح ويحذف المستند (ولكن لا يستطيع حذف الـ Auth من الواجهة الأمامية بشكل افتراضي).
