# تقرير شامل لمشروع لوحة تحكم NetCard Pro (Admin Web)

## 1. نظرة عامة على النظام
نظام **NetCard Pro - Admin Web** هو لوحة تحكم إدارية (Dashboard) متكاملة مبنية بتقنيات الويب الحديثة، تهدف إلى إدارة شبكات توزيع البطاقات/الرصيد. يعمل النظام كنقطة تحكم مركزية لمديري النظام (Admins) والموظفين (Staff) لإدارة شبكة العملاء (Network Owners)، متابعة المبيعات، حساب العمولات، إدارة الاشتراكات، والتحكم في إعدادات التطبيق الخاص بالعملاء (Android App).

---

## 2. الهيكلة التقنية (Technical Architecture)

### واجهة المستخدم (Frontend)
* **إطار العمل:** React 18 مع TypeScript.
* **نظام البناء (Build Tool):** Vite (لضمان سرعة التطوير والبناء).
* **التوجيه (Routing):** React Router DOM (للتنقل بين الصفحات كنظام SPA - Single Page Application).
* **التصميم والتنسيق:** Tailwind CSS (لبناء واجهات متجاوبة، سريعة، وعصرية).
* **الأيقونات:** Lucide React.
* **إدارة التواريخ:** مكتبة `date-fns` مع دعم اللغة العربية.

### الواجهة الخلفية وقاعدة البيانات (Backend & Database - BaaS)
* **المزود:** Firebase (من Google).
* **قاعدة البيانات:** Firestore (NoSQL Real-time Database).
* **المصادقة:** Firebase Authentication (باستخدام البريد الإلكتروني وكلمة المرور في الخلفية).
* **التخزين:** Firebase Storage (لرفع صور الإعلانات واللافتات).

---

## 3. هيكل قاعدة البيانات (Firestore Database Schema)

يعتمد النظام على هيكلة NoSQL محسّنة لتجنب التكرار ولتسهيل الاستعلامات المعقدة:

### أ. مجموعة المستخدمين `users`
المجموعة المركزية التي تضم جميع حسابات النظام (المديرين، الموظفين، والعملاء).
* **الحقول الأساسية:** 
  * `name`: اسم المستخدم.
  * `phone`: رقم الهاتف (يستخدم لتسجيل الدخول).
  * `pin`: الرمز السري (4 أرقام).
  * `role`: دور المستخدم (`ADMIN`, `STAFF`, `NETWORK_OWNER`).
  * `is_active` / `isActive`: حالة الحساب (نشط/موقوف).
* **حقول خاصة بالعملاء (`NETWORK_OWNER`):**
  * `commission_rate`: نسبة عمولة النظام من مبيعات العميل.
  * `subscription_end_date`: تاريخ انتهاء الاشتراك بصيغة Timestamp.
  * `storeName`: اسم المتجر أو الشبكة.
* **حقول خاصة بالموظفين (`STAFF`):**
  * `permissions`: مصفوفة (Array) تحتوي على الصلاحيات (مثل: `clients`, `sales`, `settings`...).

### ب. مسار المبيعات `networks/{userId}/sales/{saleId}`
يتم كتابة المبيعات من قبل **تطبيق الأندرويد** في هذا المسار.
لوحة التحكم تستخدم استعلام `collectionGroup('sales')` لقراءة جميع المبيعات من كافة الشبكات دفعة واحدة.
* **الحقول الأساسية:** `cardId`, `customerId`, `faceValue` (القيمة المالية)، `createdAt` (تاريخ الإنشاء)، `status` (حالة العملية مثل `COMPLETED`).

### ج. إعدادات النظام `app_settings/global_config`
وثيقة واحدة (Singleton) تتحكم في التطبيق بالكامل.
* **الحقول:**
  * `is_app_active`: إيقاف أو تشغيل تطبيق العملاء بالكامل.
  * `maintenance_message`: رسالة الصيانة.
  * `WebView_Config`: إعدادات رابط الويب ومكان ظهوره في التطبيق.
  * `Banners`: مصفوفة روابط لصور الإعلانات المرفوعة على Firebase Storage.
  * `Current_Popup`: الرسالة المنبثقة الحالية (العنوان والنص).

---

## 4. شاشات النظام ووظائفها (System Screens)

1. **شاشة تسجيل الدخول (Login):**
   * تسجيل دخول برقم الهاتف ورمز PIN مكون من 4 أرقام.
   * يقوم النظام برمجياً بدمج الرقم السري مع كلمة ثابتة (`kayan`) لمصادقة Firebase.

2. **اللوحة الرئيسية (Dashboard):**
   * عرض إحصائيات سريعة (الاشتراكات النشطة، العمولات المعلقة، مبيعات اليوم والشهر).
   * وصول سريع للإجراءات (إضافة عميل، إضافة جهاز).

3. **إدارة العملاء (Clients) والملف الشخصي (Client Profile):**
   * عرض جميع العملاء، إضافتهم، وإيقاف/تنشيط حساباتهم.
   * صفحة الملف الشخصي تعرض: حالة الاشتراك، إجمالي المبيعات، العمولات المستحقة، وسجل مبيعات العميل بالتفصيل.

4. **المبيعات (Sales):**
   * استعراض جميع المبيعات التي تتم عبر تطبيق الأندرويد في الوقت الفعلي (Real-time).

5. **الاشتراكات (Subscriptions):**
   * تتبع تواريخ انتهاء اشتراكات العملاء وإمكانية تجديدها وتنبيه المنتهية اشتراكاتهم.

6. **العمولات (Commissions):**
   * حساب أرباح النظام (الإدارة) من مبيعات العملاء بناءً على نسبة العمولة (`commission_rate`) المحددة لكل عميل.

7. **إدارة الموظفين (Employees):**
   * إضافة موظفين مساعدين للمدير، وتحديد صلاحياتهم بدقة (مثلاً: السماح برؤية المبيعات ومنعهم من الإعدادات).

8. **إعدادات النظام (System Settings):**
   * التحكم المركزي بتطبيق الأندرويد (تفعيل/إيقاف، إرسال رسائل منبثقة، تغيير رابط WebView، ورفع إعلانات البانر).

---

## 5. آلية المصادقة والأمان

* **المصادقة (Authentication):**
  الواجهة تطلب من المستخدم `رقم الجوال` + `الرمز السري (4 أرقام)`. في الخلفية يقوم النظام بتحويلها إلى:
  * الإيميل: `phone@kayansoft.com`
  * الباسورد: `pin+kayan` (مثال: `1234kayan`).
* **إدارة الجلسات:** تُحفظ بيانات المستخدم الأساسية في `localStorage` (`kayan_user`) مع وجود مستمع حي (Real-time Listener) للتحقق الدائم من حالة الحساب. إذا تم إيقاف الحساب من مدير آخر، يُطرد المستخدم فوراً.
* **قواعد أمان قاعدة البيانات (Firestore Rules):**
  تم تأمين القاعدة بحيث:
  * يُمنع تعديل أو حذف المبيعات بعد إنشائها.
  * لا يتمكن من الوصول للمعلومات الحساسة إلا من كان مسجلاً للدخول وتم التحقق من هويته (`request.auth != null`).

---

## 6. ارتباطات النظام (System Integrations)

* **التكامل مع تطبيق الأندرويد:** 
  * يقرأ تطبيق الأندرويد وثيقة `global_config` ليعرف حالة النظام (صيانة أم يعمل)، الإعلانات، والرسائل المنبثقة.
  * يكتب تطبيق الأندرويد عملياته في `networks/{userId}/sales`، وتلتقطها لوحة التحكم الفورية.
* **معالجة الملفات (Storage):**
  استخدام Firebase Storage لرفع البانرات الإعلانية وتخزينها، ثم حفظ الرابط المباشر (Download URL) في Firestore.

---
*تم إنشاء هذا التقرير بواسطة AI Studio Coding Agent في تاريخ 24-08-2026*
